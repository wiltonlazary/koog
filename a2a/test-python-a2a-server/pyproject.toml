[project]
name = "test-python-a2a-server"
version = "0.1.0"
description = "Python A2A server for integration tests."
requires-python = ">=3.12"
dependencies = [
    "a2a-sdk[http-server]==0.3.22",
    "uvicorn==0.35.0",
]
